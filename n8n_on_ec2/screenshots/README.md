# Screenshots

Add screenshots here and reference them from the project README:

- `ec2_instance.png` — the EC2 instance summary (type, AMI, Elastic IP)
- `security_group.png` — the inbound rules (22 / 80 / 443 only)
- `route53_record.png` — the `n8n` A record in the `builtbydhruv.com` hosted zone
- `caddy_cert_logs.png` — `docker compose logs caddy` showing the certificate obtained
- `n8n_login.png` — `https://n8n.builtbydhruv.com` serving the n8n UI over HTTPS
- `padlock.png` — browser address bar showing the valid Let's Encrypt certificate
